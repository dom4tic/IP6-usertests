export { LegendConfig }

/**
 * @typedef LegendConfigType
 * @property { string } targetElement
 */

/**
 *
 * @param { string } targetElement
 * @return { LegendConfigType }
 * @constructor
 */
const LegendConfig = targetElement => ({
    targetElement
});
